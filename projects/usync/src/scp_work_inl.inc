#include "usync_config.h"



static void scp_one_file(const std::string& path, const std::string& file) {
    g_usyncConfig.localDir;
    g_usyncConfig.remoteDir;
    g_usyncConfig.sshHost;
    g_usyncConfig.sshPort;
    g_usyncConfig.sshUserName;
    g_usyncConfig.sshPassword;


}

